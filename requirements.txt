# Versions alignées sur l'environnement d'entraînement (picklable sur Streamlit Cloud)
streamlit>=1.40.0
yfinance>=0.2.50

# Stack ML — versions identiques à celles utilisées pour l'entraînement
# (les .pkl sérialisés avec sklearn 1.8 ne se chargent pas avec 1.5.x)
numpy>=2.0
pandas>=2.2
scikit-learn>=1.6
xgboost>=2.1
lightgbm>=4.5
optuna>=4.0

# Features & analyse
ta==0.11.0
plotly>=5.24
openpyxl>=3.1
sqlalchemy>=2.0
apscheduler>=3.11
python-dotenv>=1.0
loguru>=0.7
tenacity>=9.0
joblib>=1.4
requests>=2.32

# UI / viz
matplotlib>=3.9
seaborn>=0.13
kaleido==0.2.1

# Divers
email-validator>=2.2
vaderSentiment==3.3.2

# Dev/test (installés uniquement en local via `pip install -r requirements-dev.txt` idéalement)
pytest>=8.3
pytest-cov>=6.0
